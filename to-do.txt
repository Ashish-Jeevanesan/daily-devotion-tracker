-- Add dark mode ** Done ** 13-01
-- Add feature to Copy the devotion ** Done **
-- Notification feature neeeds to be planned. ** Planned Need to execute **
-- Add Forgot Password option to reset Password ** Done **
-- Add a popup to show when sign in is done to check mail for confirmation. ** Done ** 14-01
-- Add view Password ** Done ** 15-01
-- Multi Language Support 
-- UI Enhancements ** Done ** 18-01
-- User needs to view the dates he Missed there devotion. ** Done **
-- The bible books and verses must be in a selectable wheel. ** Done ** 
-- Need to create a Trello board to record the updates.
-- Mobile view book selector is very clumsy
-- Mail redirection link is localhost only ** Fixed **
-- Mail Templates for Sign UP and Forget Password Changes ** Fixed ** 
-- Admin Page needs to be planned and developed. ** Done ** 
-- Reports must be mailed to all users via Cron Job.
-- Add notes type window on add devotion.
-- Access Control Refactor Plan ** Done **
1. Add master access table `access_rules` to hold all permission codes. ** Done **
2. Add mapping table `profile_access_rules` to assign rules to users/profiles. ** Done **
3. Seed the first access rule: `run_user_report_job`. ** Done **
4. Add RLS policies so users can read only their own access mappings and admins can manage/report on them. ** Done **
5. Add Angular access constants and an `AccessService` for loading/checking current-user permissions. ** Done **
6. Keep existing `role`-based admin route behavior unchanged for now and layer permission checks for new features. ** Updated to access-rule model **
7. Add a `UserReportJobService` that invokes the Supabase edge function `user-report-job`. ** Done with `monthlt-report-api` **
8. Add a gated "Run User Report Job" action in Admin Reports, visible only when the mapped access exists. ** Moved to Profile Center and completed **
9. Add loading/error/success UX around the job trigger. ** Done **
10. Verify the Angular build after the access-control and report-job changes. ** Done **
11. Add permission-based calendar user-switching with `calender_admin_view`. ** Done **
12. Add permission-based user-access mapping UI with `map_user_access`. ** Done **
13. Add report frequency selection in Profile Center and persist to `profiles.report_preference`. ** Done **
14. Add mobile-friendly Profile Center navigation. ** Done **
-- Notification Execution Plan
1. Add `notification_enabled`, `notification_time`, and `notification_timezone` to `profiles`.
2. Create `push_subscriptions` table for per-device/browser subscriptions.
3. Create `notification_log` table for deduplication and delivery tracking.
4. Add RLS policies for subscription and notification log tables.
5. Enable Angular PWA/service worker support.
6. Add notification settings to Profile Center.
7. Request browser permission and register push subscription using `SwPush`.
8. Upsert active browser subscriptions into `push_subscriptions`.
9. Build `send-reminders` edge function with timezone-aware scheduling.
10. Skip users who already completed the expected devotion/check-in.
11. Prevent duplicate sends using `notification_log`.
12. Void expired/dead push subscriptions on permanent delivery failure.
13. Add Supabase cron scheduling for reminder execution.
14. Test on desktop Chrome first, then Android Chrome.
